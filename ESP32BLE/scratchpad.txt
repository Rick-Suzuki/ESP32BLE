In settings, in keyboard settings, underneath the two buttons
add some text that says custom keyboard delay.


Then on the left underneath that have a "key down" label,
on the right add a picker box


, then the key up and then a picker box.
separate into 2 screens



screen 1 main
top right  = goto settings


function keys
contains a 20 button grid
labelled F1 to F20
when f1 btn is pressed, send text f1


modifier toggle buttons at the bottom
cmd, option, shift, control, off

when toggled on
 if cmd toggle is on, send cm
 option = op
shift = sh
control = ct
off send off



settings screen
tod left = goto main screen

be able to connect to esp32
take most of the avaible code and use for connect, code stays the same

move "MacOS ks" and "PS ks" buttons onto this screen

make the twenty btns F1-F20 fill the available area.
btns should be: white texst, black background, white border, size 1 px.
text font should bigger.
there should be no gap between the btns.
btn corner radius should be zero

** hello from iOS

the text for buttons F1-F20 is contained in a file in the app home directory called fnkeys.txt

when the app starts, load the btn txt from this file.

if the file doesn't exist create it first


add a table to the right in settings
the table shows the contents of doc dir
press an item in table loads it into the f1-f20 buttons
if there are not enough items, still create the btns, just leave the text blank
if there are too many items, ignore the overlfow
the table items should have black backgrounds

ask me any questions if anything is unclear











