Files changed:
ESP32BLE/Scrns/Set_Screen/Set_Screen.swift

Build result:
Project built successfully.

Delete All Data fix:
Delete All Data now enumerates all top-level contents of the Documents directory with no extension filtering, no hidden-file skipping, and no regular-file-only guard.

Delete behaviour:
Every item in Documents is removed with FileManager.removeItem(at:), so files and directories are removed recursively. The Documents directory itself is preserved.

Logging added:
DELETE_ALL_TRACE logs:
Documents path
contents before delete
every remove attempt
every remove success
every remove failure
contents after delete
refresh after delete

Post-delete:
Settings state is reset and the document list refresh still runs after deletion.
