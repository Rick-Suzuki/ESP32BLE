Implemented the final iOS 17 Settings top-button fix.

1. Reusable UIKit component used:
SettingsUIKitVisualButton in Set_TxtEditor.swift􀰓.

It wraps UIButton through UIViewRepresentable, and the UIButton owns:
title, background color, title color, enabled state, alpha, corner radius, border, content insets, font, and .touchUpInside.

2. Exact five controls converted on iOS 17:
Main, File Mode, Repair, and New through SettingsToolbarButton.
Filename through settingsTitleControl.

3. Original dimensions/appearance preserved:
Main/File Mode/Repair/New use the same titles, colors, enabled states, font, border, corner radius, padding, and toolbar positions. iOS 17 now uses fixed UIKit button frames matching the existing toolbar sizes: Main 92 x 36, others 64.4 x 36.

Filename keeps its own titleWidth x 36 sizing and gray/border visual design.

4. Newer iOS unchanged:
On iOS 18 and newer, the original SwiftUI Button implementations and styling modifiers are still used.

5. Original action/state closures unchanged:
Main still calls saveAndReturnToMain().
Filename still sets documentNameDraft and isEditingDocumentName.
File Mode still runs the same list-mode cycling logic.
Repair still calls repairDocument().
New still calls createNewDocument().

6. Obsolete experimental code removed:
Removed the unreliable SettingsCompatibilityTextButton path.
Removed the temporary NakedUIKitSettingsToolbarButton experiment.

Useful action logs remain for the final physical test.

7. Build result:
Project built successfully.

Build log:
/var/folders/kj/8r83ch8d6dx88328fxn6sf5m0000gn/T/ActionArtifacts/1C6CE6DD-BE88-4D6E-A1BA-8A9B72F3264A/BuildProject/BuildProject-Log-20260925-154648.txt
